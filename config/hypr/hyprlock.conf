# ==================== Binary Harbinger's Hyprlock Configuration ====================

source = ./theme.conf

general {
    grace = 1
    ignore_empty_input = true
    text_trim = false
    disable_loading_bar = true
}

background {
    color = rgba(222222FF)
    path = $HOME/.config/hypr/wallppr.png
    blur_size = 2
    blur_passes = 3
    zindex = -1
}

input-field {
    monitor =
    size = 320, 45
    outline_thickness = 1
    dots_size = 0.25
    dots_spacing = 0.2
    dots_center = true
    outer_color = $accent
    inner_color = $bg_dark
    font_color = $accent
    fade_on_empty = true
    fade_timeout = 1000
    placeholder_text = <i><span foreground="##b4befeaa">Input Password...</span></i>
    hide_input = false
    rounding = 5
    check_color = $check
    fail_color = $fail
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_transition = 300
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    position = 0, -80
    halign = center
    valign = center
    zindex = 10
}

# TIME HR
label {
    text = cmd[update:1000] echo -e "$(date +"%H")"
    color = $accent
    font_size = 150
    font_family = AlfaSlabOne
    position = 0, -100
    halign = center
    valign = top
    shadow_passes = 3
    shadow_size = 3
    shadow_color = $shadow
    shadow_boost = 1.2
    zindex = 3
}

# TIME
label {
    text = cmd[update:1000] echo -e "$(date +"%M")"
    color = $accent
    font_size = 150
    font_family = AlfaSlabOne
    position = 0, -270
    halign = center
    valign = top
    shadow_passes = 3
    shadow_size = 3
    shadow_color = $shadow
    shadow_boost = 1.2
    zindex = 3
}

# DATE
label {
    text = cmd[update:1000] echo -e "$(date +"%d %b %A")"
    color = $accent
    font_size = 14
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    halign = center
    valign = center
    shadow_passes = 3
    shadow_size = 3
    shadow_color = $shadow
    shadow_boost = 1.2
    zindex = 3
}

# Big Rectangle
shape {
    size = 100%, 60
    color = $bg_dark
    halign = center
    valign = bottom
    zindex = 0
}

# Small Rectangle for Battery
shape {
    size = 70, 32
    rounding = 12
    color = $bg_gray
    halign = right
    valign = bottom
    position = -14, 14
    zindex = 1
}

# Battery Status
label {
    text = cmd[update:5000] ~/.config/hypr/scripts/lockscripts.sh -bat
    shadow_passes = 1
    shadow_boost = 0.5
    color = $accent
    font_size = 14
    font_family = Maple Mono
    position = -21, -8
    halign = right
    valign = bottom
    zindex = 2
}

# Small Rectangle for Session
shape {
    size = 150, 32
    rounding = 10
    color = $bg_gray
    halign = center
    valign = bottom
    position = 0, 14
    zindex = 1
}

# Current Session Status
label {
    text = cmd[update:24000000] echo "Session : $XDG_SESSION_DESKTOP"
    color = $accent
    font_size = 12
    font_family = Jost Medium
    position = 0, -5
    halign = center
    valign = bottom
    zindex = 2
}

# PFP Image
image {
    path = $HOME/.face
    size = 80
    rounding = 10
    border_size = 3
    border_color = $accent
    position = 10, 10
    halign = left
    valign = bottom
    zindex = 3
}

# Username
label {
    text = $USER
    shadow_passes = 1
    shadow_boost = 0.5
    color = $accent
    font_size = 14
    font_family = Jost Bold Italic
    position = 120, 28
    halign = left
    valign = bottom
    zindex = 2vim.opt.clipboard = "unnamedplus"
}

# Hostname
label {
    text = cmd[update:24000000] echo "@$(uname -n)"
    shadow_passes = 1
    shadow_boost = 0.5
    color = $accent
    font_size = 14
    font_family = Jost Bold Italic
    position = 120, -20
    halign = left
    valign = bottom
    zindex = 2
}

# Lock Icon
label {
    text = 
    shadow_passes = 1
    shadow_boost = 0.5
    color = $accent
    font_size = 20
    font_family = Font Awesome 6 Free Solid
    position = 0, -65
    halign = center
    valign = top
    zindex = 2
}

# PLAYER IMAGE
image {
    path = $HOME/.config/hypr/hyprlock/music.webp
    size = 60
    rounding = 5
    border_size = 0
    reload_time = 5
    reload_cmd = ~/.config/hypr/scripts/lockscripts.sh -music --arturl
    position = -106, -230
    halign = center
    valign = center
    zindex = 1
}

# PLAYER BOX
shape {
    color = $bg_dark
    size = 300, 100
    rounding = 10
    position = 0, -230
    halign = center
    valign = center
    zindex = 0
}

# PLAYER TITTLE
label {
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/lockscripts.sh -music --title)"
    color = $accent
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 50, -242
    halign = center
    valign = center
    zindex = 1
}

# PLAYER STATUS
label {
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/lockscripts.sh -music --status)"
    color = $accent
    font_size = 18
    font_family = JetBrains Mono Nerd Font Mono Bold
    position = -50, -250
    halign = center
    valign = center
    zindex = 1
}

# PLAYER SOURCE
label {
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/lockscripts.sh -music --source)"
    color = $accent_fade
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono
    position = -20, -214
    halign = center
    valign = center
    zindex = 1
}

# PLAYER Artist
label {
    text = cmd[update:1000] echo "$(~/.config/hypr/scripts/lockscripts.sh -music --artist)"
    color = $accent_dim
    font_size = 12
    font_family = JetBrains Mono Nerd Font Mono
    position = 10, -265
    halign = center
    valign = center
    zindex = 1
}
